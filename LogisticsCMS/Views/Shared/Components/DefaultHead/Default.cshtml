<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="msapplication-TileColor" content="#0f2b4a">
    <meta name="description" content="Lojistik çözümlerinizi tek noktadan yönetin">
    <meta name="keywords" content="lojistik, kargo, taşımacılık, sevkiyat">
    <meta name="author" content="">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/templates/assets/imgs/template/favicon.svg">
    <!-- Bootstrap Grid only (for col/row/container utilities) -->
    <link href="/templates/assets/css/vendors/bootstrap.min.css" rel="stylesheet">
    <!-- Theme CSS (Swiper, old classes still used by some components) -->
    <link href="/templates/assets/css/style.css?v=3.0.0" rel="stylesheet">
    <!-- Modern overrides (top of CSS cascade) -->
    <link href="~/css/home-modern.css" rel="stylesheet" asp-append-version="true">
    <style>
        body, button, input, select, textarea, .font-heading,
        .main-menu li a, .megamenu li a, .menu-text {
            font-family: 'Manrope', sans-serif !important;
        }
        h1,h2,h3,h4,h5,h6, .hero-title, .title-favicon,
        .color-brand-2, .section-title {
            font-family: 'Manrope', sans-serif !important;
            letter-spacing: -0.02em;
        }
    </style>
    <title>LojiS — Lojistik CMS</title>
</head>
